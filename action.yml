name: Kubectl utility
description: Use bitnami's latest kubectl with your Github actions
author: Nicholas Gronow <nick@wov.io>
branding:
  icon: terminal
  color: blue
inputs:
  config_data:
    description: The kube config data base64 encoded
    required: true
runs:
  using: docker
  image: docker://ghcr.io/innovaciongascaribe/kubectl:1.21.14
